// Opgave: brug Math.ceil til at runde op til næste hele tal.
// Eksempel: 4.1 skal blive til 5.

// så Math.ceil() runder altid op til nærmeste heltal.
export function rundOp(tal: number): number {
    // Skriv din løsning her.
    return Math.ceil(tal);
}
