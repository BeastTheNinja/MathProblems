// Opgave: kast en terning antalKast gange.
// Find gennemsnittet af kastene og rund det med Math.round.
// Brug Array.from, Math.random, reduce og Math.round.

export function terningGennemsnit(antalKast: number): number {
    void antalKast;
    return 0;
}
