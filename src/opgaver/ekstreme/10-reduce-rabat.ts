// Opgave: en pris får flere rabatter efter hinanden.
// Brug reduce til at beregne den nye pris efter hver rabat.
// Rund kun det endelige resultat ned med Math.floor.
// Eksempel: 100 med [10, 20] skal give 72.

export function prisMedRabatter(pris: number, rabatter: number[]): number {
    void pris;
    void rabatter;
    return 0;
}
