// Opgave: lav en rangering af scores fra størst til mindst.
// Returner et nyt array, så det oprindelige array ikke ændres.
// Brug spread, sort og en sammenligningsfunktion.

export function rangering(scores: number[]): number[] {
    void scores;
    return [];
}
