// Opgave: find gennemsnittet af en liste og rund det op.
// Eksempel: [10, 11, 12] skal give 11.

export function afrundetGennemsnit(tal: number[]): number {
    // Brug reduce til summen og Math.ceil til sidst.
    void tal;
    return 0;
}
