// Opgave: beregn samlet tid i hele minutter.
// Hvert tal er tid i minutter, og hver aktivitet skal rundes op separat.
// Eksempel: [2.1, 4.8] skal give 7.
// Brug map, Math.ceil og reduce.

export function samletHeleMinutter(minutter: number[]): number {
    void minutter;
    return 0;
}
