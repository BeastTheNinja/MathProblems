// Opgave: rund hver pris ned til nærmeste hele krone,
// og læg derefter alle priserne sammen.
// Eksempel: [10.9, 4.2] skal give 14.
// Brug map, Math.floor og reduce.

export function samletHelePris(priser: number[]): number {
    void priser;
    return 0;
}
