// Opgave: læg alle tal sammen, find gennemsnittet,
// og rund gennemsnittet til nærmeste hele tal.
// Brug reduce og Math.round.

export function afrundetGennemsnit(tal: number[]): number {
    void tal;
    return 0;
}
